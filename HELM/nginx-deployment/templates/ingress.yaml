apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qacand-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
    - host: yeswanth.cars
      http:
        paths:
        - path: /nexon
          pathType: Prefix
          backend:
              service:
                name:  nginx-service
                port:
                  number: 8083
        
